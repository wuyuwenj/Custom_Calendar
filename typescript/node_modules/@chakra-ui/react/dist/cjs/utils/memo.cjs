"use strict";
'use strict';

const memo = (fn, getKey) => {
  const cache = /* @__PURE__ */ Object.create(null);
  function get(...args) {
    const key = getKey?.(...args) ?? args.join("|");
    if (cache[key] === void 0) cache[key] = fn(...args);
    return cache[key];
  }
  return get;
};

exports.memo = memo;
